#!/usr/bin/env bash
# Run Home Assistant from this checkout for live testing.
# HA binds 8123 inside the container; VS Code / container run forwards it to the host.
set -euo pipefail

cd "$(dirname "$0")/.."

if [ ! -d config ] || [ ! -L "config/custom_components/kakao_map" ]; then
  echo "config/ not initialized — running scripts/setup first."
  scripts/setup
fi

exec hass --config "$PWD/config" --debug
