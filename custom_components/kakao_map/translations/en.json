{
  "config": {
    "step": {
      "user": {
        "description": "Enter your Kakao Developers REST API key. It is validated with a single keyword search call.",
        "data": {
          "api_key": "REST API key"
        }
      }
    },
    "error": {
      "invalid_auth": "The API key was rejected by Kakao (HTTP 401).",
      "cannot_connect": "Could not reach the Kakao API. Check your network and try again."
    },
    "abort": {
      "already_configured": "Kakao Map is already configured. Only one entry is supported."
    }
  },
  "services": {
    "search_place": {
      "name": "Search place",
      "description": "Searches Kakao Map for a place by keyword and returns the top result with coordinates, addresses, and map links.",
      "fields": {
        "query": {
          "name": "Query",
          "description": "Search keyword, e.g. a place or business name."
        }
      }
    },
    "get_directions": {
      "name": "Get directions",
      "description": "Builds a Kakao Map route link between an origin and destination (with optional waypoints) and returns the per-leg points. Travel time is added in a later step.",
      "fields": {
        "origin": {
          "name": "Origin",
          "description": "An entity holding the origin location (person, device_tracker, zone, sensor, ...) or a latitude/longitude value."
        },
        "destination": {
          "name": "Destination",
          "description": "An entity holding the destination location or a latitude/longitude value."
        },
        "waypoints": {
          "name": "Waypoints",
          "description": "Up to 5 stops, each an entity or a latitude/longitude value. Not allowed with the public transit mode."
        },
        "mode": {
          "name": "Travel mode",
          "description": "car, traffic (public transit), walk, or bicycle."
        }
      }
    },
    "patch_map": {
      "name": "Patch map tiles (experimental)",
      "description": "Swaps the default Home Assistant map tiles for Kakao tiles by editing the frontend bundle. A backup is kept for restore_map. Restart HA and clear the browser cache afterwards."
    },
    "restore_map": {
      "name": "Restore map tiles",
      "description": "Restores the original Home Assistant map tiles from the backups created by patch_map."
    }
  },
  "selector": {
    "mode": {
      "options": {
        "car": "Car",
        "traffic": "Public transit",
        "walk": "Walk",
        "bicycle": "Bicycle"
      }
    }
  },
  "exceptions": {
    "no_results": {
      "message": "No places found for \"{query}\"."
    },
    "api_error": {
      "message": "The Kakao API request failed. Check the log for details."
    },
    "point_input_missing": {
      "message": "Specify an entity or coordinates for {role}."
    },
    "entity_not_found": {
      "message": "Entity {entity_id} was not found."
    },
    "entity_missing_coordinates": {
      "message": "Entity {entity_id} has no latitude/longitude attributes."
    },
    "invalid_location": {
      "message": "Invalid location for {role}: {value}. Expected a latitude/longitude pair."
    },
    "traffic_no_waypoints": {
      "message": "Public transit routes do not support waypoints. Remove the waypoints or choose another mode."
    },
    "too_many_waypoints": {
      "message": "Too many waypoints ({count}). Kakao Map allows at most {max}."
    },
    "map_patch_failed": {
      "message": "The map tile patch could not be applied: {error}"
    }
  }
}
